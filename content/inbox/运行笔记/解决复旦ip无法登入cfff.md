- 按下 `Win + R`，输入 `ncpa.cpl`。
- 找到连接校园网的网卡（WLAN 或以太网），右键 **属性**。
- 双击 **Internet 协议版本 4 (TCP/IPv4)**。
- 即使现在是自动获取，请尝试手动改为复旦官方 DNS：
    - **首选 DNS 服务器**：`61.129.42.6`
    - **备用 DNS 服务器**：`202.120.224.6`
- 保存后在终端运行 `ipconfig /flushdns`。

总结: 问题出在DNS上面. 在复旦的内网却无法解析复旦ip的DNS, 结果导致服务器拒绝访问. 