# 安装Conda
通过
```bash
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh
```
安装miniconda(woc怎么下载这么慢啊啊啊啊)

使用清华镜像源成功下载. 

# 下载SRA-toolkit
使用清华镜像
```bash
# 使用清华镜像的操作
# 1. 添加 bioconda 频道 (生物信息学软件)
conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud/bioconda/

# 2. 添加 conda-forge 频道 (bioconda 的主要依赖库)
conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud/conda-forge/

# 3. 添加 defaults (官方主频道、R语言频道等)
conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/main/
conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/r/
conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/free/

# 4. 设置搜索时显示频道地址，方便确认
conda config --set show_channel_urls yes
```
进行操作
```bash
conda create -n sra_env
conda activate sra_env
conda install sra-tools
```
经过验证下载成功. 
# 下载单细胞数据
```bash
# 创建并进入单细胞数据目录
mkdir -p ~/GSE181294_analysis
cd ~/GSE181294_analysis
# 安装e-utils
conda install -c bioconda entrez-direct
# 开始后台下载单细胞数据
nohup prefetch --max-size 200G --option-file sample.txt &
```